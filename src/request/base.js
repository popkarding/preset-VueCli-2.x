/**
 * 接口域名管理
 */
// 关联 ENV
const base = {
  // serverRequest: process.env.BASE_URL,
  // buildRequest: process.build.BASE_URL,
  // test: process.test.BASE_URL
  serverRequest: 'https://xxxxxxxx.com/api/v1',
  buildRequest: 'http://xxxxxxxx.com/api',
  test: '/api'
}

export default base
